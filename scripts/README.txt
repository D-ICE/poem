install.sh
    Installs Docker Rootless

uninstall.sh
    Uninstalls Docker Rootless

package.sh
    Builds pypoem using cibuildwheel against the current Python version in the environment and install it using pip
    so that it Python tests in tests/pypoem can be run out of the box
